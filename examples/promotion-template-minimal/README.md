# Minimal promotion template example

A deliberately small two-language example for theme authors. It uses the same
bundled component runtime as the default theme and contains no direct platform
API logic.

Its manifest includes Chinese import-prefill metadata, but it is not currently
one of the numbered downloadable artifacts in `artifacts/catalog.json`.
