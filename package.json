{
  "name": "@parloq-flow/promotion-kit",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npm run build && node scripts/preview.mjs",
    "validate": "npm run validate:template && npm run validate:integrations",
    "validate:template": "node packages/cli/src/index.mjs template validate themes/white-label-account-link",
    "validate:integrations": "node packages/cli/src/index.mjs integration validate examples/promotion-integration-script-demo && node packages/cli/src/index.mjs integration validate examples/promotion-integration-feedback-demo && node packages/cli/src/index.mjs integration validate examples/promotion-integration-iframe-demo && node packages/cli/src/index.mjs integration validate integrations/device-callback-adapter",
    "pack": "npm run build && npm run verify:dist",
    "check:scripts": "node --check scripts/artifact-catalog.mjs && node --check scripts/build.mjs && node --check scripts/preview.mjs && node --check scripts/verify-dist.mjs && node --check packages/cli/src/index.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test packages/cli/test/*.test.mjs",
    "verify:dist": "node scripts/verify-dist.mjs",
    "ci": "npm run check:scripts && npm run typecheck && npm test && npm run build && npm run verify:dist"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "ajv": "^8.17.1",
    "esbuild": "^0.25.9",
    "fflate": "^0.8.2",
    "libphonenumber-js": "1.13.11",
    "typescript": "~5.7.2"
  }
}
